<views:MvxWindowsPage
    x:Class="Sample.WinPhone81.Views.FirstView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:views="using:MvvmCross.WindowsCommon.Views"
    mc:Ignorable="d">

    <Grid Background="{StaticResource ApplicationPageBackgroundThemeBrush}">
		   <StackPanel>
                <TextBlock Text="{Binding DeviceInfo.DeviceType}" />
                <TextBlock Text="{Binding DeviceInfo.SoftwareVersion}" />
                <TextBlock Text="{Binding DeviceInfo.HardwareVersion}" />
                <TextBlock Text="{Binding DeviceInfo.Manufacturer}" />
                <TextBlock Text="{Binding DeviceInfo.DeviceName}" />
                <TextBlock Text="{Binding DeviceInfo.HardwareId}" />
                <TextBlock Text="{Binding Status}" />
            </StackPanel>
    </Grid>
</views:MvxWindowsPage>
